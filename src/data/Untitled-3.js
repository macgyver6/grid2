/** Class representing a point. */
class Point {
  /**
   * Create a point.
   * @param {Object} properties
   * @param {Number} properties.x - The x value.
   * @param {Number} properties.y - The y value.
   */
  constructor(properties) {
    // ...
  }
}

var x = new Point({});
